const {assert} = require('chai')
const request = require('supertest')
const {jsdom}  = require('jsdom')

const app = require('../../app')

describe('Server Path: /register', () => {
  describe('POST', () => {
    
  }
}
